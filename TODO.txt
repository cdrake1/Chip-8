tdl
-focus on getting monitor to work. (upd- Successfully created one monitor object and shared it to CPU) still broken

to run the website, run the command **npm install -g http-server**, then cd into **web** and run http-server

1. build out and test mem
2. build out cpu and test - decode is done working on execute
3. test c bat file - file broken must run the file tsc line manually in /chip-8
4. fix monitor and test
5. update read me
